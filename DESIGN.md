# wake-up-yesterday 设计笔记（D 自用·脱敏）

## 一句话
每天固定时间（北京时间凌晨 6 点）自动换窗：归档 → 交接包 → 开新窗 → 切守望者 → 读档长回来。人类全程不用动手。

## 实现要点（从咱家 doorman/window-health 实践里提炼）

- 守望者每 10s 轮询三房间，注入到当前 SESSION（`/api/session.prompt`，mode=queue）
- 换窗 = 改 SESSION 常量 + 精确 kill 旧进程（排除自身 PID）+ 同命令重启 + 验证注入成功才推进已读
- 交接包生成：换窗前自动落盘当天重要事 + 生成"昨日说啥做啥待办"摘要，换完窗先读它
- 每日 6:00 自动触发；`--dry-run` 预演不改配置

## 已实现（2026-08-30 更新，脚本见 auto-swap-window.mjs）

- ✅ 定时触发：常驻进程每分钟查北京时间 6:00（`--now` 立即执行、`--dry-run` 预演）
- ✅ 交接包模板：日期 / 昨日要点（自动提取记忆条目）/ 待办 / 规矩 / 接力卡
- ✅ 换窗后自检：注入新窗"读交接包长回"提示（必须实际读，不能拿"文件存在"冒充）
- ✅ 守护配套：health-check 每 5 分钟扫进程，缺了自动拉起、多了去重（防双实例）

## 配套脚本（同思路，见各自仓库/文章）

- 分层记忆：core 永亮 / recent 当前 30 条 / archive 全量检索（memory-layers）
- 每日回顾：晚安触发，自动总结当天入档
- 守望者自愈：doorman 死了 5 分钟内被 health-check 自动拉起

## 安全

- 仓库内脚本已脱敏：路径用 `{MM}`/`{MEMORY_JSON}` 占位、密钥 `***`，按需替换
- 密钥、token、密码、私话一概不进公开仓库
